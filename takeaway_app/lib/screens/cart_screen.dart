// Removed: CartScreen
// Kept as a placeholder file — cart functionality is not part of the current authentication-focused flow.

import 'package:flutter/material.dart';

class CartScreenPlaceholder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Cart screen removed')));
  }
}
