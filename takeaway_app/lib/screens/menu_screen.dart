// Removed: MenuScreen
// This file has been cleared because the app now focuses on the authentication flow (Splash, Sign In, Sign Up, Forgot Password).
// Kept for historical reference. If you need the menu functionality restored, revert this file from source control or ask to regenerate it.

import 'package:flutter/material.dart';

class MenuScreenPlaceholder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Menu screen removed')));
  }
}
