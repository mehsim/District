// Removed: CheckoutScreen
// Kept as placeholder. Checkout flow not part of the current authentication flow.

import 'package:flutter/material.dart';

class CheckoutScreenPlaceholder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Checkout screen removed')));
  }
}
